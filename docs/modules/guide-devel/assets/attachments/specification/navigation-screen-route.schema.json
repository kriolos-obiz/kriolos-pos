{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "navigation-screen-route.schema.json",
  "title": "ScreenRoute",
  "description": "Immutable value object representing a navigable screen destination in KriolosPOS.",
  "type": "object",
  "required": ["taskClass"],
  "properties": {
    "taskClass": {
      "type": "string",
      "description": "Fully-qualified Java class name used as the CardLayout key and ViewManager lookup key.",
      "minLength": 1,
      "pattern": "^[a-zA-Z_][a-zA-Z0-9_.]*$",
      "examples": [
        "com.openbravo.pos.sales.JPanelTicket",
        "com.openbravo.pos.inventory.StockManagement"
      ]
    },
    "title": {
      "type": "string",
      "description": "Human-readable screen title displayed in the content title bar. Locale-agnostic key resolved by the Swing adapter via AppLocal. Empty string if no title.",
      "default": ""
    }
  },
  "additionalProperties": false,
  "examples": [
    {
      "taskClass": "com.openbravo.pos.sales.JPanelTicket",
      "title": "Sales"
    },
    {
      "taskClass": "com.openbravo.pos.inventory.StockManagement",
      "title": "Stock Management"
    }
  ]
}
